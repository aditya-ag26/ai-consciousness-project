# Full development environment: API, data pipeline, evaluation, and tests.
#
# Only direct dependencies are listed; pip resolves the rest. requirements-api.txt
# is the smaller subset the container needs to serve traffic.

# --- Web API ---
fastapi==0.141.1
uvicorn==0.52.1
python-dotenv==1.1.1
PyYAML==6.0.2
pydantic==2.11.7

# --- RAG pipeline ---
langchain==0.3.26
langchain-community==0.3.27
langchain-core==0.3.86
faiss-cpu==1.11.0

# --- Embedding backends (selected by EMBEDDING_BACKEND) ---
# onnx: what the container runs. torch: convenient locally, and what the
# original index was built with. Both produce identical vectors.
fastembed==0.8.0
langchain-huggingface==0.3.0
sentence-transformers==5.0.0
torch==2.7.1

# --- Language model backends (selected by LLM_PROVIDER) ---
langchain-google-genai==2.1.12
langchain-ollama==0.3.3

# --- Data pipeline (src/data) ---
pandas==2.3.1
pyarrow==20.0.0

# --- Tests ---
pytest==9.1.1
httpx==0.28.1
